The vllm installation should not be skipped. If a Python version outside of >=3.10,<3.14 is detected, then install a Python version within these boundaries. Use pyenv.

---

Make sure that pyenv gets installed in the first place.